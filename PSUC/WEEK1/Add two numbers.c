#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b;
    printf("Enter the two numbers: ");
    scanf("%d%d", &a,&b);
    printf("The sum of two numbers is %d", a+b);
    return 0;
}
